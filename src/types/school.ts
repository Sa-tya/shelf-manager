export interface School {
  id: number;
  name: string;
  location: string;
  contact: string;
}

export interface SchoolFormData {
  id?: number;
  name: string;
  location: string;
  contact: string;
} 